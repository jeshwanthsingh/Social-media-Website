<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Application for CSC317</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/public/css/index.css">
    </head>
    <body>
        {{> header}}
        {{> navbar}}
        
        <main>
            <div class="main-content">
                <h2>Latest Posts</h2>
                
                <form class="search-form" action="/search" method="GET">
                    <input type="text" id="searchTerm" name="q" required>
                    <input type="submit" value="Search">
                </form>

                <div class="posts">
                    {{#each posts}}
                        <div class="post">
                            <h2>{{this.title}}</h2>
                            <img src="{{this.thumbnailPath}}" alt="Post thumbnail">
                            <p>{{this.content}}</p>
                            <a class="view-button" href="/viewpost/{{this.id}}">View post</a>
                        </div>
                    {{/each}}
                </div>
            </div>
        </main>

        {{> footer}}

        <script src="/public/js/index.js" async defer></script>
    </body>
</html>
